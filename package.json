{
  "name": "fcc-headerparser-microservice",
  "version": "0.0.1",
  "description": "fcc header parser microservice",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && bower install",
    "devmode": "nodemon ./server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.2"
  }
}
